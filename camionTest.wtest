import cosas.*
import camion.*

//test


describe "group of test for camion" {

  test "first test" {
    assert.that(true)
  }
  test "camion con bumblebee robot" {
    bumblebee.transformarRobot()
    camion.aumentarCarga(bumblebee)
    assert.that(camion.pesoTotal() == 1800 && camion.todasCosasConNivelDePeligroMayor(1).contains(bumblebee))
  }
  

}